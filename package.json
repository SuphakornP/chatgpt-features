{
  "name": "chatgpt-features",
  "version": "1.0.0",
  "private": true,
  "description": "Thai-first interactive guide to ChatGPT features, sourced from official ChatGPT documentation.",
  "scripts": {
    "dev": "python3 -m http.server 4173",
    "validate": "node tools/validate.mjs",
    "check:syntax": "node --check assets/js/content.js && node --check assets/js/app.js",
    "test": "npm run validate && npm run check:syntax"
  },
  "engines": {
    "node": ">=18"
  }
}
